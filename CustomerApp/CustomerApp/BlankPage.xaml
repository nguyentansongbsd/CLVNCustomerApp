<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:CustomerApp.Controls"
    x:Class="CustomerApp.BlankPage" BackgroundColor="White">
    <ContentPage.Content>
        <AbsoluteLayout>
            <StackLayout AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All">
                <Label Text="Du an"/>
                <controls:LookUp x:Name="lookupDuAn" Placeholder="du an" Margin="0,0,0,5" BottomModal="{Reference LookupModal}" NameDisplay="bsd_name" SelectedItem="{Binding Project}" ItemsSource="{Binding Projects}"/>

                <Button Text="Chon hinh anh" Clicked="Button_Clicked_1"/>
                
                <Button Text="test notification" Clicked="Button_Clicked" />
            </StackLayout>
            <controls:BottomModal x:Name="LookupModal"/>
        </AbsoluteLayout>

    </ContentPage.Content>
</ContentPage>
